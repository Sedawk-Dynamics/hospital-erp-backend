node_modules/
dist/
dist-seed/
.env
.env.local
.env.production
*.log
coverage/
.nyc_output/
uploads
platform-config.tmp-*

# Vendor drug catalogue workbooks (660 MB). build-drug-catalog.py turns them
# into prisma/scripts/data/drug-catalog, which is what ships.
/data/
