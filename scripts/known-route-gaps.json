{
  "//": "Frontend calls with no matching route that are ACCEPTED for now, because each needs a product decision rather than a path fix. A ratchet, like the ESLint block: never add an entry to make a build pass, and delete one the moment it is fixed. `npm run check:routes` fails on anything new AND on an entry left here after it is fixed. Recorded 2026-09-09; the seven nursing-forms entries were fixed the same day.",
  "known": [
    {
      "method": "POST",
      "path": "/auth/2fa/disable",
      "reason": "Patient portal offers a Disable 2FA button; the server has only /2fa/setup and /2fa/verify, so it toasts a failure. Build the endpoint or remove the button - not a path fix."
    }
  ]
}
